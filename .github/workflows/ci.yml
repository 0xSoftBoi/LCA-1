name: verification

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: lca1-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHONHASHSEED: "0"

jobs:
  model-and-corpus:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12.12"
      - name: Record Python version
        run: python3 --version
      - name: Compile Python
        run: python3 -m compileall -q model bench tests tools
      - name: Validate fabrication and package release
        run: make fabrication-check
      - name: Unit and randomized differential tests
        run: python3 -m unittest discover -s tests -v
      - name: Check generated corpus drift
        run: python3 tools/gen_vectors.py --check
      - name: Workload manifest smoke test
        run: python3 -m bench.bridge_profile --profile authenticated

  license-compliance:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12.12"
      - name: Install pinned REUSE tool
        run: python3 -m pip install reuse==6.2.0
      - name: REUSE specification lint
        run: reuse lint

  rtl-formal-synthesis:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24.14.0"
          cache: npm
      - name: Install pinned Yosys runtime
        run: npm ci --ignore-scripts
      - name: Install RTL simulator
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends iverilog
      - name: Record host tool versions
        run: |
          mkdir -p reports
          node --version | tee reports/node-version.txt
          npm --version | tee reports/npm-version.txt
          iverilog -V > reports/iverilog-version.txt 2>&1
      - name: Full-width generated RTL regression
        run: make rtl-test
      - name: NTT synchronous SRAM protocol regression
        run: |
          iverilog -g2012 -Irtl -s lca_ntt_protocol_tb \
            -o reports/ntt_protocol.vvp \
            integration/openframe/lca_sram_1rw.sv rtl/lca_ntt_accel.sv verification/tb_ntt_protocol.sv
          vvp reports/ntt_protocol.vvp | tee reports/ntt-protocol.txt
      - name: Rev-A core protocol regression
        run: |
          iverilog -g2012 -Irtl -s lca_reva_core_smoke_tb \
            -o reports/reva_core_smoke.vvp \
            integration/openframe/lca_sram_1rw.sv rtl/lca_ntt_accel.sv rtl/lca_keccak_f1600.sv rtl/lca_secure_sram.sv \
            integration/openframe/lca_reva_core.sv verification/tb_reva_core_smoke.sv
          vvp reports/reva_core_smoke.vvp | tee reports/reva-core-smoke.txt
      - name: NTT independent Yosys elaboration
        run: >-
          node --experimental-wasm-exnref tools/run_yosys.mjs
          -l reports/ntt-yosys-elaboration.log
          -p 'read_verilog -sv -Irtl integration/openframe/lca_sram_1rw.sv rtl/lca_ntt_accel.sv; prep -top lca_ntt_accel; check'
      - name: Rev-A core independent Yosys elaboration
        run: >-
          node --experimental-wasm-exnref tools/run_yosys.mjs
          -l reports/reva-core-yosys-elaboration.log
          -p 'read_verilog -sv -Irtl integration/openframe/lca_sram_1rw.sv rtl/lca_ntt_accel.sv rtl/lca_keccak_f1600.sv rtl/lca_secure_sram.sv integration/openframe/lca_reva_core.sv; prep -top lca_reva_core; check'
      - name: Optional independent Verilator front-end
        run: bash verification/lint/run_lint.sh
      - name: Formal gates
        run: make formal
      - name: Generic structural synthesis
        run: make synth
      - name: Archive verification and synthesis evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: lca1-e1-evidence-${{ github.sha }}
          path: reports/
          if-no-files-found: warn
          retention-days: 30

  ntt-pqclean-differential:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout pinned algorithm references
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install CXXRTL toolchain
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends yosys g++
      - name: Record differential tool versions
        run: |
          mkdir -p reports
          yosys -V | tee reports/differential-yosys-version.txt
          g++ --version | head -n1 | tee reports/differential-gxx-version.txt
      - name: Differential NTT and Keccak against pinned PQClean
        run: make -f Makefile.fullchip test-datapaths | tee reports/pqclean-datapath-differential.txt
      - name: Archive differential evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: lca1-pqclean-differential-${{ github.sha }}
          path: reports/
          if-no-files-found: warn
          retention-days: 30
