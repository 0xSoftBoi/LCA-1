[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lca1"
version = "0.1.0"
description = "Evidence-gated architecture models for the LCA-1 lattice coprocessor"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
lca-bridge-profile = "bench.bridge_profile:main"

[tool.setuptools]
packages = ["model", "bench"]
